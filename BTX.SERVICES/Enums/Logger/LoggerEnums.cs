namespace BTX.SERVICES.Enums.Logger
{
    public static class LoggerEnums
    {
        public enum OutPutFileType { Text, Console, Database }
        public enum MessageType { Message, Error, Warning }
    }
}
